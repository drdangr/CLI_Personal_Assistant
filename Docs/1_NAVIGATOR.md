# 🗺️ Навигатор улучшений - С чего начать?

## Для новичка: Где найти нужную информацию?

### 📚 Я хочу быстро разобраться в проекте (5 минут)
👉 Откройте **README.md** → раздел **"🚀 Для новых разработчиков"**

Там вы найдёте:
- Порядок чтения файлов
- Ключевые паттерны
- Примеры кода

---

### 🔍 Я не понимаю, что делает функция `parse_input()`
👉 Откройте **cli.py** → функция `parse_input()` (строки 17-36)

Там вы найдёте:
- Примеры парсинга команд
- Объяснение что такое `shlex.split()`
- Примеры с кавычками

```python
'add "John Doe" 1234567890' → cmd='add', args=['John Doe', '1234567890']
```

---

### 🎯 Я не понимаю, что такое `@input_error` и `@mutating`
👉 Откройте **commands.py** → строки 78-140

**`@input_error`** (строки 78-115):
- Перехватывает KeyError, ValueError, IndexError
- Выводит дружные сообщения об ошибках
- Пример использования

**`@mutating`** (строки 118-140):
- Автоматически сохраняет на диск
- Когда НЕ сохраняет (ошибки, exit)
- Примеры

---

### 📞 Я не понимаю регулярное выражение для телефона
👉 Откройте **config.py** → строки 10-12

```python
# Паттерн для телефона: ^ (начало) + ровно 10 цифр + $ (конец)
# Примеры: '1234567890' ✓, '123' ✗, '12345678901' ✗
PHONE_REGEX = r"^\d{10}$"
```

---

### 📅 Логика расчёта дней до дня рождения непонятна
👉 Откройте **models.py** → методы класса `Record`

**`days_to_birthday()`** (строки 181-205):
- Пошаговый алгоритм (1, 2, 3, 4 шага)
- Практические примеры
- Inline комментарии в коде

**`get_next_birthday()`** (строки 207-222):
- Аналогично, но возвращает дату

---

### ➕ Я хочу добавить новую команду
👉 Откройте **README.md** → раздел "🚀 Для новых разработчиков" → "Как добавить новую команду?"

Вы найдёте пример за 30 секунд:
```python
@REG.register("new-cmd", help='...')
@input_error
@mutating
def cmd_new_cmd(args, storage):
    return "Результат"
```

---

### 🧪 Как мне протестировать код?
👉 Откройте **README.md** → раздел "🚀 Для новых разработчиков" → "Как протестировать код?"

Примеры тестирования:
- Валидация одного поля
- Тест команды
- Полный тест



## 📂 Структура документации

```
f_project/
├── README.md (главный файл)
│   └─ Раздел "🚀 Для новых разработчиков"
├── 1_NAVIGATOR.md (Быстрый навигатор по проекту)
├── 2_SUMMARY.md (Краткое саммари проекта)
├── 3_ARCHITECTURE.md (Описание архитектуры проекта)
├── 4_COMMANDS.md (Полный разбор самого сложного модуля - обработчика команд)
│
└── Исходный код с комментариями:
    ├── config.py (regex объяснены)
    ├── cli.py (parse_input() документирована)
    ├── commands.py (@input_error, @mutating описаны)
    └── models.py (Record, дни рождения объяснены)
```



## ❓ Частые вопросы

**Q: С чего начать?**
A: Читайте в порядке: README → config.py → storage.py → models.py → commands.py. 
   По необходимости - читайте документы в папке Docs в том порядке, в котором они пронумерованы

**Q: Где найти пример новой команды?**
A: В README.md раздел "Как добавить новую команду"

**Q: Непонятно regex в config.py**
A: Откройте config.py, там есть примеры валидных/невалидных значений

**Q: Как работает `@input_error`?**
A: В commands.py есть docstring с примерами

**Q: Как рассчитываются дни до ДР?**
A: В models.py метод `days_to_birthday()` с пошаговым объяснением
